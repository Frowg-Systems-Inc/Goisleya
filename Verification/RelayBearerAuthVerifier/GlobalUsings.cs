// Mirrors the Microsoft.NET.Sdk.Web implicit global usings that the compiled-in
// Isley.Relay sources rely on (the verifier itself uses the plain SDK).
global using Microsoft.AspNetCore.Http;
global using Microsoft.Extensions.Logging;
